import type { KnowledgePack } from "../types";

export const brandStrategyKnowledgePack: KnowledgePack = {
  id: "brand-strategy-knowledge",
  domain: "brand_strategy",
  principles: ["separate name from style", "define energy", "define audience", "create distinct creative territories"],
  patterns: ["territory by symbol system", "territory by motion/energy", "territory by premium restraint"],
  antiPatterns: ["generic adjectives only", "three near-identical variants", "turning audience words into brandName"],
  checklists: ["brandName exact", "visual constraints captured", "territories distinct", "industry signal included"],
  examples: ["sportif ELEVIO keeps ELEVIO as brandName and uses performance as style."],
};
